# Copy this file to .env for local development.

# Optional display name for welcome text (future use)
FRIDAY_USER_NAME=Datta

# Optional override for local data file path (future use)
# Example: ./data.json
FRIDAY_DATA_PATH=./data.json

# Runtime environment
NODE_ENV=development
